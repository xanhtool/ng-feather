{
  "name": "ng-feather",
  "version": "0.0.1",
  "scripts": {
    "postversion": "ng build ng-feather --prod && git push && git push --tags && npm run publish"
  },
  "peerDependencies": {
    "@angular/common": "^9.0.6",
    "@angular/core": "^9.0.6",
    "tslib": "^1.10.0"
  }
}
